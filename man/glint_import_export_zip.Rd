% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{glint_import_export_zip}
\alias{glint_import_export_zip}
\title{Extract the export ZIP and load its CSVs.}
\usage{
glint_import_export_zip(resp, encoding = "UTF-8", combine = TRUE)
}
\arguments{
\item{resp}{httr response object containing the ZIP bytes.}

\item{encoding}{Character encoding to pass to \code{\link[readr:read_csv]{readr::read_csv()}}.}

\item{combine}{Controls multi-CSV handling. When \code{TRUE} (the default and
backward-compatible behavior), CSVs sharing an identical column schema
are stitched together with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}. When \code{FALSE}, every
multi-CSV export is returned as a named list of data frames keyed by
filename (without \code{.csv}), regardless of schema match. Single-CSV
exports are always returned as a single data frame.}
}
\description{
Extract the export ZIP and load its CSVs.
}
\keyword{internal}
